package org.example.laba.repository;

import org.example.laba.domain.EncodingKey;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EncodingKeyRepository extends JpaRepository<EncodingKey, Long> {
}
